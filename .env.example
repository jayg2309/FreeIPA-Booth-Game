# ── Supabase (required for shared leaderboard) ──
# Get these from: Supabase Dashboard → Project → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key

# ── Admin PIN (required for /admin page) ──
ADMIN_PIN=your-secret-pin-here

# ── OpenAI (optional — enables AI-generated questions) ──
# Get a key from: https://platform.openai.com/api-keys
# Without this, the game falls back to the static 50-question bank.
OPENAI_API_KEY=sk-your-key-here
